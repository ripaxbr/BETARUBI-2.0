# Sentry — BETARUBI 2.0

A aplicação já possui integração com `sentry-sdk[flask]`.

Configure `SENTRY_DSN` e `SENTRY_TRACES_SAMPLE_RATE` nas variáveis de ambiente da Vercel. O código não contém DSN ou credenciais.
