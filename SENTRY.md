# Sentry — WEBPLAY

A aplicação já possui integração com `sentry-sdk[flask]`.

Configure `SENTRY_DSN` e `SENTRY_TRACES_SAMPLE_RATE` nas variáveis de ambiente do ambiente de execução. O código não contém DSN ou credenciais.
